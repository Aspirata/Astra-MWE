// User-facing reference text, kept out of the application controller.
export const helpContent = /* HTML */ `<div class="help-content">
  <h3>Открытие и экспорт</h3>
  <p>
    Выберите мир из списка или папку с level.dat. Выберите клиентский JAR соответствующей версии.
    Добавьте JAR модов и ZIP ресурспаков: нижние строки переопределяют верхние. Задайте область и
    экспортируйте GLB. В 3D загружается окружение центра выделения: до 256 × 256 блоков с запасом
    128 блоков (8 чанков) с каждой стороны; дальние участки ограничены бюджетом памяти. Размер
    области экспорта не ограничивает навигацию по карте. В виде сверху карта поверхности заполняет
    окно и подгружается при перемещении.
  </p>
  <h3>Управление как в Blender</h3>
  <p>
    СКМ — орбита. Shift + СКМ — сдвиг. Колесо — масштаб, Ctrl + СКМ — плавный dolly. Numpad 1 / 3 /
    7 — спереди / справа / сверху, Ctrl — с противоположной стороны. Numpad 5 — перспектива /
    ортографическая проекция. Numpad . или F — показать выделение.
  </p>
  <p>
    Оси в правом нижнем углу повторяют поворот камеры. Нажмите X, Y или Z, чтобы открыть вид вдоль
    оси; повторное нажатие показывает обратную сторону. Малые кружки открывают отрицательные
    направления. Ось Y — высота Minecraft.
  </p>
  <p>
    Shift + ~ — полёт от текущей камеры. Мышь — обзор, WASD — перемещение, Q / E — вниз / вверх,
    Shift — ускорение. Esc — выйти из полёта.
  </p>
  <h3>Выделение сверху</h3>
  <p>
    ЛКМ с перетаскиванием — прямоугольная область, СКМ — сдвиг. Плоская карта поверхности
    подгружается по границам окна независимо от выделения. При отдалении детализация карты
    уменьшается, при приближении восстанавливается. Ранее открытые участки сохраняются в кеше.
    Значки с лицами игроков видны сверху. Изменения выделения автоматически готовят только область
    экспорта. Цвета биомов всегда включены, смешивание — 7 блоков. Автоматическая высота
    ограничивает область по рельефу с запасом снизу минимум 4 блока. Отключите её для ручного
    диапазона Y.
  </p>
  <h3>Blender</h3>
  <p>
    Импортируйте GLB через File → Import → glTF 2.0. Файл содержит текстуры и отдельные материалы
    блоков. Для полупрозрачных материалов используйте Dithered в настройках Blender. Исходный мир
    открывается только для чтения.
  </p>
  <h3>О программе</h3>
  <p>
    Astra MWE 0.1.0. Возможны ошибки геометрии и неполная поддержка ресурсов модов. Лицензия MIT.
  </p>
</div>`;
